# Core ML
torch>=2.0.0
torchaudio>=2.0.0
speechbrain>=1.0.0
numpy

# ASR
faster-whisper>=1.0.0
# Optional: cần khi ASR_BACKEND=phowhisper (vinai/PhoWhisper-*) hoặc
# SPEAKER_BACKEND=wavlm (microsoft/wavlm-base-plus-sv).
# transformers + sentencepiece + safetensors (transitively).
transformers>=4.40.0
sentencepiece>=0.2.0

# Optional: chỉ cần khi SPEAKER_DENOISE=true (spectral gating denoise
# trước khi encode speaker). Bỏ qua → core/audio_io.py tự skip denoise.
# noisereduce>=3.0.0

# VAD
silero-vad>=5.0.0

# Audio I/O
sounddevice>=0.4.6
soundfile>=0.12.1
scipy

# TTS
gTTS>=2.5.0
pydub>=0.25.1
audioop-lts>=0.2.1  # compatibility shim cho Python 3.13+ (audioop bị xóa khỏi stdlib)

# NLU
google-genai>=1.0.0

# Misc
python-dotenv>=1.0.0
requests
